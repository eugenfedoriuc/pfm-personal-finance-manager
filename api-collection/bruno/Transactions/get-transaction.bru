meta {
  name: Get transaction
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/transactions/{{expenseTransactionId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.amount: eq 900.00
}
