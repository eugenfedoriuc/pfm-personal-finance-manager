meta {
  name: Get category
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/categories/{{scratchCategoryId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.name: eq Sparen
}
